# 项目说明

## 常用指令

日常记录: claude_trading "标题" --summary "..." --todos "..." --improvements "..."
查看进度: claude_todos / claude_improvements / claude_stats
确认代码状态: git log --oneline -5 && git status
提交改动: git add <file> && git commit -m "..." && git push
